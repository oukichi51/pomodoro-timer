課題①：集中ログ付きタイマー（1日の実績を見える化）

ゴール：
「今日は何分集中したか」「何回サイクル回したか」が分かるダッシュボード付きタイマーにする。

仕様イメージ

タイマー終了のたびに、localStorage にログを保存

日付

モード（集中 / 休憩）

開始時刻 / 終了時刻

所要時間（秒）

画面の下部に「今日の実績」を表示

今日の合計集中時間（分）

今日の完了サイクル回数（集中終了の数）

できれば日付跨ぎも対応

0:00を跨いだら自動的に「今日」の集計対象が変わる

手ごたえポイント

データ設計（どんな構造でlocalStorageに持つか）

「今のタイマー状態」と「過去ログ」の2種類の状態管理

日付ごとのフィルタリング＆集計ロジック

発展：
簡単な棒グラフを自前で描いてみる
（SVGでも、ただのdivの高さを変えるだけでもOK）。

課題②：デスクトップ通知 + タブを閉じても使える（PWA入門）

ゴール：
ブラウザのタブを見てなくてもちゃんと気づけるタイマーにする。

ステップざっくり

Notification API でデスクトップ通知

初回起動時に Notification.requestPermission() で許可をもらう

タイマー終了時に new Notification("集中おわり！休憩に入りましょう")

ページが非アクティブでも見える工夫

document.title に残り時間を出す（25:00 | 休憩タイマー みたいな）

PWA化（余裕あれば）

manifest.json を追加（アイコン / 名前 / スタートURL）

service worker を登録して、オフラインでも最低限動くようにする

スマホのホーム画面に追加してアプリっぽく使えるように

手ごたえポイント

ブラウザAPI（Notification, Page Visibility, Service Worker）への理解

ファイル構成の整理（manifest.json, sw.js など）

「Webサイト」から一歩進んで「Webアプリ」に近づく感じ

課題③：タイマー設定の「プリセット管理」機能

ゴール：
ユーザーが「25-5」「50-10」みたいなセットをいくつか保存して、ワンクリックで切り替えられる。

仕様イメージ

画面に「プリセット一覧」を追加

例:

① 通常ポモドーロ（集中25 / 休憩5）

② ディープワーク（集中50 / 休憩10）

ユーザーがフォームから

プリセット名

集中分

休憩分
を入力して追加

選択したプリセットがタイマーの設定欄に反映される

プリセットは localStorage に保存され、ブラウザを閉じても残る

手ごたえポイント

複数設定の管理（配列・IDの扱い）

「選択中のプリセット」と「現在のモード・時間」の整合性管理

UI的にも少しだけ複雑になる（選択, 追加, 削除）

課題④：GitHub Pages ＋ Actions で「デプロイ自動化」

これはアプリ機能というより開発プロセス寄りの課題。

ゴール：

main に push すると GitHub Actions が

Lint or 簡単なテスト（あれば）

zip作成（今のやつ）

GitHub Pages に自動デプロイ
までやってくれる

やること

gh-pages ブランチ or docs/ ディレクトリ公開設定

Actions で npm なしの静的サイトデプロイ

成果物zip作成ジョブとデプロイジョブを両方走らせる

手ごたえ：
「CI/CD 触りました」と言えるレベルの内容になる。